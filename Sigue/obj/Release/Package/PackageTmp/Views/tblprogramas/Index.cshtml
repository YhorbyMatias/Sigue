@model IEnumerable<Sigue.Models.tblprograma>

@{
    ViewBag.Title = "Mantenimiento programa";
}

<h2>Mantenimiento programa</h2>

<p>
    @Html.ActionLink("Crear nuevo", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Programa)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.tblfacultad.Facultad)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Programa)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.tblfacultad.Facultad)
        </td>
        <td>
            @Html.ActionLink("Editar", "Edit", new { id=item.ProgramaId }) |
            @Html.ActionLink("Detalles", "Details", new { id=item.ProgramaId }) |
            @Html.ActionLink("Eliminar", "Delete", new { id=item.ProgramaId })
        </td>
    </tr>
}

</table>
